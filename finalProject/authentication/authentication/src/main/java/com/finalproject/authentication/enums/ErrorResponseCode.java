package com.finalproject.authentication.enums;

public enum ErrorResponseCode {

    BAD_CREDENTIALS,
    TOKEN_STILL_VALID,
    REFRESH_TOKEN_INVALID,
    USER_CREDENTIALS_NOT_FOUND,
    ROLE_NOT_FOUND
    }
